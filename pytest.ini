[pytest]
# pytest configuration for ecommerce ELT pipeline tests

# test discovery — only look in tests/ folder
testpaths = tests
pythonpath = elt

# show extra summary for failures, errors, warnings
addopts = -v --tb=short

# log level for captured logs in tests
log_cli = false
log_level = INFO